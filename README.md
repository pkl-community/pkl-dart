# Pkl Bindings for Dart

> Status on Features:
> * Evaluator API: Unimplemented
> * Generator API: Unimplemented
> * External Reader API: Unimplemented

This repo contains unofficial bindings for Pkl from Dart, used to work with, generate types from and embed Pkl into Dart Applications.

> Support for Flutter is unknown

You've done a great work on this. 

I haven't gone through it deeply, but here are a few things I noticed:
1. You might need to change the headings for your files (headings should be at the top-level of the file -> , 
and as it is now a community project, we can now attach the copyright to "The Pkl Community Authors". You can then add an AUTHORS file or similar and put your name and 